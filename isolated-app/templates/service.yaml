apiVersion: v1
kind: Service
metadata:
  name: {{ include "isolated-app.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "isolated-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isolated-app.selectorLabels" . | nindent 4 }}